# E2E test user (copy to .env.e2e and fill in; .env.e2e is gitignored)
# Optional: only needed for the "authenticated user sees dashboard after login" test.
E2E_TEST_EMAIL=
E2E_TEST_PASSWORD=

MONGO_URI=